master:
  args:
    - --users=500
    - --run-time=640
    - --spawn-rate=25
  environment:
    # run-time = 10m + users / spawn-rate + clear-after
    CLEAR_STATS_AFTER_SPAWNING: 20

worker:
  replicas: 12
