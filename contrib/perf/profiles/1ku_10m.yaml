master:
  args:
    - --users=1000
    - --run-time=670
    - --spawn-rate=25
  environment:
    # run-time = 10m + users / spawn-rate + clear-after
    CLEAR_STATS_AFTER_SPAWNING: 30

worker:
  replicas: 20
