master:
  args:
    - --users=2000
    - --run-time=720
    - --spawn-rate=25
  environment:
    # run-time = 10m + users / spawn-rate + clear-after
    CLEAR_STATS_AFTER_SPAWNING: 40

worker:
  replicas: 30
